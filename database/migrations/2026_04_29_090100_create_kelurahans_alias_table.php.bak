<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;
use Illuminate\Support\Facades\DB;

return new class extends Migration
{
    /**
     * Run the migrations.
     * Creates a view 'kelurahans' that maps to the 'branch' table for backward compatibility
     *
     * @return void
     */
    public function up()
    {
        DB::statement("
            CREATE VIEW kelurahans AS
            SELECT 
                id,
                branch as kelurahan,
                id_branch as id_kecamatan,
                deleted_at,
                created_at,
                updated_at
            FROM branch
        ");
    }

    /**
     * Reverse the migrations.
     *
     * @return void
     */
    public function down()
    {
        DB::statement('DROP VIEW IF EXISTS kelurahans');
    }
};
